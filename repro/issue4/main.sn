fn main(): void =>
    var s: str = "Hello"
    var c: char = s.charAt(0)

    # None of these work:
    var asString: str = c.toString()
    var lower: char = c.toLower()
    var code: int = c.toInt()

    print($"{asString}\n")
