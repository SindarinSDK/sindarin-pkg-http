# Reproduction: Calling function-typed struct fields

struct Handler =>
    callback: fn(int): int

fn multiply(x: int): int =>
    return x * 2

fn main(): void =>
    var h: Handler = Handler { callback: multiply }
    var result: int = h.callback(5)
    print($"Result: {result}\n")
