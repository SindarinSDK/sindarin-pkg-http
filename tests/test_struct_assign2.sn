# Test struct field assignment - different approaches

struct Point =>
    x: int
    y: int

    fn setValues(a: int, b: int): void =>
        self.x = a
        self.y = b

fn main(): void =>
    var p: Point = Point { x: 0, y: 0 }
    p.setValues(10, 20)
    print($"x={p.x}, y={p.y}\n")
